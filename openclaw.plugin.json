{
  "id": "stella-timeline-plugin",
  "name": "Stella Timeline Plugin",
  "version": "2.8.10",
  "description": "OpenClaw timeline runtime with canonical timeline_resolve and timeline_transition, LLM-based temporal reasoning, and guarded append-only writes.",
  "skills": [
    "skills/timeline-skill"
  ],
  "contracts": {
    "tools": [
      "timeline_resolve",
      "timeline_transition"
    ]
  },
  "activation": {
    "onStartup": true
  },
  "configSchema": {
    "type": "object",
    "properties": {
      "enableTrace": {
        "type": "boolean",
        "default": true,
        "description": "Whether Timeline writes trace records for each runtime execution."
      },
      "traceLogPath": {
        "type": "string",
        "description": "Optional trace log path. Relative paths are resolved from the workspace."
      },
      "canonicalMemoryRoot": {
        "type": "string",
        "description": "Root directory for canonical timeline daily logs. Relative paths are resolved from the workspace."
      },
      "enablePromptTimelineContext": {
        "type": "boolean",
        "default": true,
        "description": "Whether to inject timeline prompt context through before_prompt_build."
      },
      "promptTimelineLookbackDays": {
        "type": "integer",
        "default": 7,
        "description": "How many days read_only_fast and prompt timeline context may look back for still-active macro events."
      },
      "promptTimelineMacroThresholdMinutes": {
        "type": "integer",
        "default": 120,
        "description": "Duration threshold above which an active fact is treated as macro background rather than an instantaneous scene."
      },
      "promptTimelineDirectCurrentStateAnswers": {
        "type": "boolean",
        "default": true,
        "description": "Whether active_instant prompt context may authorize direct answers for current-state questions without a full timeline_resolve call."
      },
      "reasonerTimeoutMs": {
        "type": "integer",
        "default": 90000,
        "description": "Timeout for the internal timeline reasoner subagent, in milliseconds."
      },
      "reasonerSessionPrefix": {
        "type": "string",
        "default": "timeline-reasoner",
        "description": "Session key prefix used for the internal timeline reasoner subagent."
      },
      "reasonerMessageLimit": {
        "type": "integer",
        "default": 24,
        "description": "Maximum number of messages retained when reading the internal reasoner transcript."
      },
      "sessionHistoryLimit": {
        "type": "integer",
        "default": 12,
        "description": "Maximum number of current-session messages retained by the collector."
      },
      "memorySearchMaxResults": {
        "type": "integer",
        "default": 6,
        "description": "Maximum number of results requested when the collector calls OpenClaw memory_search."
      },
      "proactiveGreeting": {
        "type": "object",
        "description": "Configuration for the proactive greeting engagement loop.",
        "properties": {
          "enabled": {
            "type": "boolean",
            "default": false,
            "description": "Enable the proactive greeting flow."
          },
          "idleThresholdHours": {
            "type": "integer",
            "default": 7,
            "description": "Minimum idle time before a proactive greeting may be considered."
          },
          "minHoursBetweenCheckins": {
            "type": "integer",
            "default": 24,
            "description": "Hard throttle between successful proactive greetings."
          },
          "minHoursSinceNonSocialOutbound": {
            "type": "integer",
            "default": 6,
            "description": "Reserved for future controlled non-social outbound throttles; ignored by proactive greeting v1."
          },
          "unansweredPenaltyThreshold": {
            "type": "integer",
            "default": 2,
            "description": "How many unanswered proactive greetings trigger the elevated idle threshold."
          },
          "unansweredPenaltyIdleHours": {
            "type": "integer",
            "default": 72,
            "description": "Idle threshold after the unanswered penalty activates."
          },
          "sessionKey": {
            "type": "string",
            "default": "proactive-greeting",
            "description": "Legacy fallback session key for pending sends created before dynamic heartbeat session tracking."
          },
          "pendingSendTimeoutMinutes": {
            "type": "integer",
            "default": 20,
            "description": "How long a pending proactive send may remain open before it is cleared as stale."
          },
          "singleUserGuard": {
            "type": "boolean",
            "default": true,
            "description": "Block proactive greeting sends when the workspace appears to serve multiple contacts."
          }
        },
        "additionalProperties": false
      }
    },
    "additionalProperties": false
  }
}
